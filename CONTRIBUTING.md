# إرشادات المساهمة والتطوير - منظومة سرايا للماشية
## Contributing Guidelines - Saraya Livestock ERP

نرحب بمساهمات فريق التطوير في بناء وتطوير منظومة سرايا. لضمان جودة واستقرار المنظومة، يرجى الالتزام بالمعايير الهندسية التالية:

---

### 1. دورة التطوير وبنية الفروع (Git Workflow)
* الفرع الرئيسي `main` مخصص للإصدارات الإنتاجية المستقرة فقط.
* فرع التطوير `develop` هو الفرع التكاملي للميزات الجديدة.
* تُنشأ الفروع وفق النمط التالي:
  * `feat/feature-name` للميزات الجديدة.
  * `fix/bug-fix-name` لإصلاح الأخطاء.
  * `refactor/component-name` لإعادة الهيكلة والتحسينات.

---

### 2. معايير الكود والجودة (Code Quality Standards)
* **TypeScript Strict Mode**: جميع الملفات يجب أن تلتزم بأنواع البيانات الصارمة دون استخدام `any`.
* **عقود الـ OpenAPI**: عند تعديل أي Endpoint في الـ API، يجب تحديث DTOs وإعادة توليد عميل الويب والتأكد من مطابقة العقود عبر:
  ```bash
  npm run contracts:check
  npm run typecheck:web
  ```
* **الاختبارات الذرية**: يجب تضمين اختبارات وحدة (Unit Tests) واختبارات تكامل (Integration Tests) لأي منطق عمل جديد في الـ API:
  ```bash
  cd apps/api && npm test -- --runInBand
  ```

---

### 3. الأمان وسلامة البيانات (Security & Data Integrity)
* يُحظر تماماً حفظ أي كلمات مرور أو مفاتيح تشفير أو أسرار داخل الكود.
* يجب استخدام المتغيرات البيئية من خلال `RuntimeConfigService` مع التحقق الصارم.
* العمليات المالية والعمليات الحساسة (الولادات، الصرف، الحلب، السجلات الطبية) يجب أن تُنفذ ضمن معاملات قاعدة بيانات ذرية (**Prisma Transactions**) ومحمية بمفاتيح عدم التكرار (**Idempotency Keys**).

---

### 4. إرسال طلبات الدمج (Pull Requests)
1. تأكد من اجتياز جميع الفحوصات محلياً (`npm run contracts:check`, `npm run typecheck:web`, `npm test`).
2. افتح Pull Request موجه إلى فرع `develop`.
3. املأ قالب الـ PR بالمعلومات المطلوبة ووصف التغييرات التي قمت بها.
4. انتظر مراجعة الزملاء واجتياز خط أنابيب GitHub Actions CI.
